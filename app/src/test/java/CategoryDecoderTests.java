import org.junit.Test;
import static org.junit.Assert.*;
import map.*;
import java.awt.Color;
import java.io.IOException;

public class CategoryDecoderTests {

    // @Test
    // public void getCategory() throws IOException {
        // ColorCategoryConversion ccc = new ColorCategoryConversion();

        // Color c = new Color(0, 0, 0);
        // String labelType = ccc.getLabelType(c);
        // assertTrue(labelType.equals("country_label : country"));

        // c = new Color(4, 0, 0);
        // labelType = ccc.getLabelType(c);
        // assertTrue(labelType.equals("marine_label : -"));

        // c = new Color(10, 0, 0);
        // labelType = ccc.getLabelType(c);
        // assertTrue(labelType.equals("place_label : suburb"));

        // c = new Color(36, 0, 0);
        // labelType = ccc.getLabelType(c);
        // assertTrue(labelType.equals("poi_label : cinema"));

        // c = new Color(255, 0, 0);
        // labelType = ccc.getLabelType(c);
        // assertTrue(labelType.equals("mountain_peak_label : volcano"));
    // }
}
